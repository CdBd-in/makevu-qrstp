# makevu-qrstp — 작업 규칙 (매 세션 자동 로드)

메이크뷰 QR 스탬프 페이지 디자인 볼트. 아래는 **AI가 항상 따라야 하는 상시 규칙**이다.

## ⭐ 최우선 규칙 — QR 이벤트 제작

1. **워크플로우 준수**: "큐알 이벤트 만들어줘" 요청은 반드시 [[큐알스탬프 솔루션/3. 워크플로우.md]] 순서대로 — Step 1(데이터 수집·질문 먼저) → Step 2(구조 확정) → Step 2.5(리소스 분기) → Step 3(Figma 제작). 곧바로 제작 착수 ❌.

2. **🔴 검수 필수 · 예외 없음**: 시안(페이지) 제작이 끝나면 **곧바로 [[큐알스탬프 솔루션/4. 검수 체크리스트.md]]로 전 항목 전수 검수**를 실행한다.
   - "완료"라고 보고하기 **전에** 반드시 검수를 통과할 것.
   - **스팟 체크(몇 페이지만 눈으로 확인)로 대체 금지.** 체크리스트 전 항목을 실제로 대조한다.
   - **각 시안마다 개별로** 검수한다(시안 2개면 2번). 복제·리테마했어도 예외 없음.
   - 스탬프는 [[큐알스탬프 솔루션/2. 작업 가이드/2-1. 페이지 구조/2-1-3. 스탬프.md]] §2-3·§2-4·§2-6까지 확인.
   - 검수 결과·수정 이력은 작업 컨텍스트 문서에 **검수 로그**로 남긴다.

3. **자주 누락되는 항목 (매번 먼저 확인)**:
   - **강조어**(장소·개수 등)에 메인 컬러 적용 — ⚠️ 텍스트를 `.characters =` 로 통짜 교체하면 **글자별 서식(강조색)이 초기화**된다. 교체 후 `getStyledTextSegments`+`setRangeFills`로 강조색 복원.
   - **스탬프 §2-6 디테일**: 그런지 브러시(①) · 면 스탬프 drop shadow · 라벨 규칙 · **inactive = `#C0C0C0` 고정**.
   - **비활성 버튼 = 활성색 opacity 30%** · **그리드 점선 정밀 사양**(2-1-3 §2-4).
   - **어두운 배경 로고 = 흰색 녹아웃**(크림 칩/프레임 ❌).
   - 수정 후에는 **스크린샷으로 실제 반영을 재확인**(해시/코드만 믿지 말 것).

## Figma 작업

- `use_figma` 호출 전 **figma-use 스킬 선로드**(MCP resource) 필수.
- `section.clone()`은 **`figma.currentPage`에 붙는다** → 다른 페이지 대상이면 같은 스크립트에서 **`setCurrentPageAsync` 먼저**.
- 이벤트별 **전용 페이지**에 작업(다른 이벤트처럼) · Cover/템플릿 페이지에 두지 말 것.
- 색·타이포·컴포넌트 스펙 단일 출처 = [[큐알스탬프 솔루션/2. 작업 가이드/2-2. 디자인 요소/2-2-1. 공통 디자인 시스템.md]].

## 경로

- Vault 파일 = 루트 기준 상대경로. 외부 경로만 절대경로.
- 파일 언급 시 위키링크(`[[...]]`)로.
